# cat = Dashboard; type = string; label = Period options for dashboard widgets (comma-separated days, e.g. 7,14,30)
dashboardPeriods = 7,14,30

# cat = Dashboard; type = int+; label = Default period in days for dashboard widgets (must be one of the configured period options)
dashboardDefaultPeriod = 7

# cat = Dashboard; type = boolean; label = Enable demo analytics data in Development context only
demoData = 0

# cat = Cache; type = int+; label = Analytics data cache lifetime in seconds (default: 3600)
pageAnalyticsCacheTtl = 3600
