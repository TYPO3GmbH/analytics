# cat = Dashboard; type = string; label = Period options for dashboard widgets (comma-separated days, e.g. 7,14,30)
dashboardPeriods = 7,14,30

# cat = Dashboard; type = int+; label = Default period in days for dashboard widgets (must be one of the configured period options)
dashboardDefaultPeriod = 7

# cat = Dashboard; type = boolean; label = Enable demo analytics data in Development context only
demoData = 0

# cat = API Configuration; type = string; label = API Base URL (leave empty for production default)
apiBaseUrl =

# cat = API Configuration; type = string; label = Analytics Data API Base URL (leave empty for production default: https://api.analytics.typo3.com/api)
analyticsApiBaseUrl =

# cat = API Configuration; type = string; label = Integration Partner ID (leave empty for default)
intpId =

# cat = API Configuration; type = boolean; label = Verify SSL Certificates (set to 0 only for local development with self-signed certificates)
verifySsl = 1

# cat = Cache; type = int+; label = Analytics data cache lifetime in seconds (default: 3600)
pageAnalyticsCacheTtl = 3600
